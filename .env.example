# Copy to .env and fill in your own local values.
#
# PostgreSQL connection string. The database must exist before running
# `npx prisma migrate dev` — create it with:
#   createdb registry
# or:
#   psql -U postgres -c "CREATE DATABASE registry"
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/registry?schema=public"
